# 版本记录

## 备份版 0.1.2 — 恢复顺序与账号核验

- 待确认的总收藏成员按已保存的自建收藏夹位置稳定排序，避免 NAS 重放顺序影响列表。
- 改用验证后的稳定账号 ID；核对旧收藏夹归属后升级旧绑定，避免 Cookie 变化被当成不同账号。
- 修复登录失败提示与提前保存凭据，取消核验可中止流程；见[版本说明](docs/releases/backup-v0.1.2.md)。

## 备份版 0.1.1 — NAS 封面恢复

- 本机没有媒体和抖音登录信息时，从 NAS 已备份图片加载封面并缓存，支持只读浏览。
- 连接恢复与刷新文件状态时重试封面；Windows 客户端更新即可，服务保持 0.1.0。
- 详见[版本说明](docs/releases/backup-v0.1.1.md)。

## 备份版 0.1.0 — 基于本地版 v0.1.7 的独立预览

- 从 v0.1.7 本地版起步，加入独立 HTTPS 备份服务、本地优先与后台增量同步。
- 加入启动比对、断线只读、续传、冲突保护、同步时间和提交电脑显示。
- 必要修复包括标题栏自动版本、已删除文件记录清理及账号一致性保护。
- 详见[备份版说明](docs/releases/backup-v0.1.0.md)。以下是本地版基点的历史记录。

## v0.1.7 — 修复封面墙缩放与窗口留白

- 内容区不再随筛选结果或本地作品数量变窄，账号与本地封面保持统一尺寸。
- 默认客户区宽度 1400 像素，封面固定 216 像素宽，窗口变化时仅调整列数。
- 底部分页和批量操作栏各有独立位置，不覆盖作品。
- 通过真实 Electron 几何测量及交互检查，详见 [版本说明](docs/releases/v0.1.7.md)。

## v0.1.6 — 收藏记录管理与紧凑界面（预览版）

- 删除账号读取记录，保留独立的本地归属和顺序；重新读取可恢复并计入新增。
- 固定每页 20 个、上下双分页、翻页回顶、筛选范围内全选。
- 紧凑筛选面板、作品区独立滚动、放大的自适应窗口。
- 统一删除确认和文件/目录选择弹窗，提示与弹窗中线对齐。
- 本地补齐先展示完整、缺失和无法检查的结果，确认后只补齐缺失内容。
- 56 项数据测试、16 项组件检查通过；本次环境未完成桌面验收，预览版不替换稳定版。见 [版本说明](docs/releases/v0.1.6.md)。

## v0.1.5 — 修复删除文件后的目录锁定

- 依据磁盘实际文件锁定目录，不再只看下载记录是否存在。
- 设置自动检查文件，并提供“重新检查文件”按钮。
- 新目录应用后清理失效文件位置，保留收藏、标签和下载历史，重新下载使用新目录。

## v0.1.4 — 按夹读取与本地浏览

- 按当前选中的收藏范围读取；部分读取按全局新增作品计数，默认 20 并记忆数量。
- 各收藏夹独立排序；自建夹作品自动加入总收藏，未知总收藏位置放入待排序分区。
- 本地单击封面浏览、双击详情，先图后视频，上下跨页浏览当前筛选结果。
- 本次运行共享音量，启动默认静音；标签弹窗筛选与我的标签快捷勾选。
- 设置齿轮配置首次下载目录；下载管理可独立删除已完成记录。
- 验证记录及具体边界见 [版本说明](docs/releases/v0.1.4.md)。

## v0.1.3 — 软件内扫码弹窗

- 参考 legacy v0.2.6 的后台网页登录页加二维码弹窗交互。
- 默认不再打开完整浏览器，扫码后自动连接并收起弹窗。
- 修复辅助 Cookie 格式导致有效登录被整体拒绝的问题。
- JSON 导入收进高级设置，明确只复用登录会话。
- 增加真实二维码获取及登录取消、按钮识别等验证。

## v0.1.2 — 系统浏览器登录与预览

- 使用系统 Chrome/Edge 登录或导入参考工具配置；凭据只在本机加密保存。
- 支持下载前视频预览，继续保留封面墙与选择下载。
- 使用服务返回游标读取收藏，不限制为17页。
- 比较静态单图实际像素并记录来源，增加更多清晰度字段支持。
- 参考项目的封面第二条地址并不保证高清原图，实测结论见参考项目核查文档。

## v0.1.1 — 登录流程修正版

- 登录窗口固定标题，区分藏匣网页登录与官方抖音客户端。
- 登录阶段不启用采集监听，检测到网页登录状态后再同步收藏。
- 移除作品解析超时后的自动刷新，避免打断扫码。
- 遇到访问频率限制时暂停自动操作，并加入至少60秒的本地退避；不会自动重试。
- 保留 v0.1.0 的文件归属、下载补齐、离线浏览与标签筛选能力。

验证：17项自动测试，以及4项原生登录隔离检查通过。平台当前登录限制是否解除、真实账号完整分页仍需验证。

## v0.1.0 — Windows 初版

- 账号收藏与本地媒体库分开，使用封面墙展示。
- 发现并选择添加自建收藏夹，保留总收藏与自建收藏夹关系。
- 按收藏夹及“标题-作者”组织文件，每个作品只保存一份。
- 支持视频、图文、静态单图、作品信息保存，以及缺失文件补齐。
- 支持原始标签、自建标签、作者和类型筛选。
- 支持单选、跨页全选、当前页视频全选。
- 检查实际文件更新“已下载”标记；远端取消收藏或失效时保留本地文件。

此版本作为历史二进制归档。完整开发源码从 v0.1.1 开始纳入版本管理，v0.1.0 标签仅提供归档说明。

## 共同的已知限制

- 收藏列表、收藏夹目录和完整分页需要真实账号登录验收。
- 测试视频可下载并以1080×1920解码；样例静态单图只有360×640，尚未确认与存图宝高清单图一致。
- 软件不会绕过登录、验证码或平台访问限制，也不会擦除作者写入画面的水印。
